import { Layout } from "@/components/Layout";
import { Grades } from "@/components/Grades";

const GradesPage = () => {
  return (
    <Layout>
      <Grades />
    </Layout>
  );
};

export default GradesPage;