import { Layout } from "@/components/Layout";
import { Notes } from "@/components/Notes";

const NotesPage = () => {
  return (
    <Layout>
      <Notes />
    </Layout>
  );
};

export default NotesPage;